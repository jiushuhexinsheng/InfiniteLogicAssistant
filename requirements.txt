# 无限逻辑·语音助手 依赖（2026-08 更新至最新版）
# 在线安装: pip install -r requirements.txt
# 离线兜底（在线不可用时）: python -m pip install --no-index --find-links=scripts\libs -r requirements.txt
# 目标环境: Python 3.14+ / Windows x64（scripts\libs 轮子按 CPython 3.14 win_amd64 打包）

httpx==0.28.1
fastapi==0.141.1
uvicorn[standard]==0.52.1
duckduckgo-search==8.1.1
pyyaml==6.0.3
loguru==0.7.3
openpyxl==3.1.5
tomli-w==1.2.0
vosk==0.3.45
sounddevice==0.5.5
mcp==2.0.0
